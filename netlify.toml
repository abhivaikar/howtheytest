[build]
  # No build needed - we're only deploying serverless functions
  # Skip package installation - not needed for functions
  command = "echo 'Functions-only deployment - no build needed'"
  publish = ".netlify-build"

[functions]
  directory = "netlify/functions"
  # Use esbuild to bundle functions with their dependencies
  node_bundler = "esbuild"

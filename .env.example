# Frontend Configuration
# Only needed if hosting static site separately from API (e.g., GitHub Pages + Netlify Functions)
# Leave blank if both are hosted together (e.g., full Netlify deployment)
NEXT_PUBLIC_API_URL=https://your-netlify-site.netlify.app/.netlify/functions/submit-resource

# GitHub App Configuration
# See CONTRIBUTION_WORKFLOW_SETUP.md for detailed setup instructions

# GitHub App ID (find in GitHub App settings)
GITHUB_APP_ID=your_app_id_here

# GitHub App Private Key (full contents of .pem file)
# Include the BEGIN and END lines
GITHUB_APP_PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----\n...\n-----END RSA PRIVATE KEY-----"

# GitHub App Installation ID (from installation URL)
GITHUB_APP_INSTALLATION_ID=your_installation_id_here

# Repository Configuration
REPO_OWNER=abhivaikar
REPO_NAME=howtheytest

# Auto-assign PRs to this GitHub username
REVIEWER_USERNAME=abhivaikar

# Security Configuration (Added for bot protection and rate limiting)
# Get free Turnstile keys from https://dash.cloudflare.com/

# Cloudflare Turnstile Secret Key (for backend verification)
# Add to Netlify environment variables
TURNSTILE_SECRET_KEY=your_turnstile_secret_key_here

# Cloudflare Turnstile Site Key (for frontend widget)
# Add to GitHub repository secrets for build workflow
NEXT_PUBLIC_TURNSTILE_SITE_KEY=your_turnstile_site_key_here

# Allowed origins for CORS (comma-separated list)
# Add to Netlify environment variables
ALLOWED_ORIGINS=https://abhivaikar.github.io,http://localhost:3000
